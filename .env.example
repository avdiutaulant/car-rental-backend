# Server configuration
# Port where the server will run (default: 3000)
PORT=3000

# Database configuration
# MongoDB connection string (replace with your database URI, this is a local example)
MONGO_URI=mongodb://localhost:27017/carRental

# JWT configuration
# Secret key for JWT authentication (replace with a secure, unique value)
JWT_SECRET=your_jwt_secret

# Copy this file to .env and replace the placeholder values with your actual configuration details.